- Terminar de montar grpc por bazel
- Tagear dependencia de modulo y usar
- Montando cliente y servidor en cada una de las 2 apps

- Revisar los go.mod (global) y dentro de cada modulo
    $ bazel run //:gazelle
    $ bazel run //:gazelle -- update-repos -from_file=go.mod -to_macro=deps.bzl%go_dependencies
    $ bazel run //:gazelle

-

